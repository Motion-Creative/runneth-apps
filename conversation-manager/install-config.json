{
  "schema": "1.0",
  "id": "conversation-manager",
  "version": "1.0.1",
  "description": "Live kanban-style conversation board for Runneth. See active conversations, ones waiting on your reply, and done conversations, with last-message previews and 10-second polling. Reads directly from the runtime database.",
  "requires": {
    "runtime": "any Runneth sandbox; Node 22",
    "preinstalled": [],
    "recommended": []
  },
  "installs": [
    {
      "file": "server",
      "destination": "/agent/apps/conversation-manager/server",
      "type": "folder-copy",
      "on-exists": "overwrite"
    },
    {
      "file": "frontend",
      "destination": "/agent/apps/conversation-manager/frontend",
      "type": "folder-copy",
      "on-exists": "overwrite"
    },
    {
      "file": "buildeth.app.json",
      "destination": "/agent/apps/conversation-manager/buildeth.app.json",
      "on-exists": "overwrite"
    }
  ],
  "post-install": [
    {
      "action": "app-create",
      "name": "Create the conversation-manager app",
      "description": "Registers the conversation-manager app container.",
      "manual": false
    },
    {
      "action": "app-build",
      "name": "Build conversation-manager",
      "description": "Installs dependencies and compiles server + frontend.",
      "manual": false
    },
    {
      "action": "app-verify",
      "name": "Verify conversation-manager",
      "description": "Confirms the build artifacts are present and the app is openable.",
      "manual": false
    },
    {
      "action": "app-url",
      "name": "Surface the conversation-manager URL",
      "description": "Returns the live route for the conversation-manager app.",
      "manual": false
    },
    {
      "action": "show-intro",
      "name": "Introduce the new capability",
      "file": "post-install-intro.md",
      "description": "Read post-install-intro.md from this use case folder. After all prior install and post-install steps complete, surface the file's sections to the user in chat as the closing message of the install turn: '## What just opened up' as prose, '## Try this now' as a numbered prompt list, '## Compounds with' only when present and at least one named use case is installed in the sandbox. Resolve any {{TOKEN}} placeholders using the same customize map applied during install."
    }
  ],
  "changelog": [
    {
      "version": "1.0.1",
      "date": "2026-05-21",
      "type": "patch",
      "notes": "Added post-install intro. After install completes, Runneth surfaces a short 'what just opened up + try this now' message sourced from post-install-intro.md."
    },
    {
      "version": "1.0.0",
      "date": "2026-05-21",
      "type": "major",
      "notes": "Initial install-config."
    }
  ]
}
