{
  "schema": "1.0",
  "id": "brand-audit",
  "version": "2.0.0",
  "description": "Foundation + strategy layer of the creative-strategy framework. Runs brand-intake, product-catalog, review-audit, brand-relevant-keywords, competitor-analysis, and creative-strategy-engine to produce a durable per-workspace brand bundle that creative-deep-dive and weekly-performance-deck read as upstream context. On-demand only: the first run asks two or three intake questions inline in chat (brand site, review sources, competitors); re-run any time to refresh — the prior bundle is archived and a short diff lands in chat. No scheduled routine.",
  "requires": {
    "runtime": "any Runneth sandbox",
    "preinstalled": [],
    "recommended": []
  },
  "installs": [
    {
      "from": "SKILL.md",
      "to": "/agent/.agents/skills/brand-audit/SKILL.md",
      "on_exists": "overwrite",
      "purpose": "Orchestrator skill — asks the first-run intake questions inline, runs the six skills in order, writes the durable bundle, installs user.md snippet"
    },
    {
      "from": "skills/brand-intake.md",
      "to": "/agent/.agents/skills/brand-intake/SKILL.md",
      "on_exists": "overwrite",
      "purpose": "Standalone callable — brand identity, value prop, audience, positioning"
    },
    {
      "from": "skills/product-catalog.md",
      "to": "/agent/.agents/skills/product-catalog/SKILL.md",
      "on_exists": "overwrite",
      "purpose": "Standalone callable — products, benefits, claims, price points"
    },
    {
      "from": "skills/review-audit.md",
      "to": "/agent/.agents/skills/review-audit/SKILL.md",
      "on_exists": "overwrite",
      "purpose": "Standalone callable — VOC across pain/desire/objection/comparison/use-case buckets"
    },
    {
      "from": "skills/brand-relevant-keywords.md",
      "to": "/agent/.agents/skills/brand-relevant-keywords/SKILL.md",
      "on_exists": "overwrite",
      "purpose": "Standalone callable — search landscape and audience language"
    },
    {
      "from": "skills/competitor-analysis.md",
      "to": "/agent/.agents/skills/competitor-analysis/SKILL.md",
      "on_exists": "overwrite",
      "purpose": "Standalone callable — competitor read with active-ad signal from motion inspo-creatives"
    },
    {
      "from": "skills/creative-strategy-engine.md",
      "to": "/agent/.agents/skills/creative-strategy-engine/SKILL.md",
      "on_exists": "overwrite",
      "purpose": "Standalone callable — pain × persona × messaging-angle matrix across 5 awareness stages"
    },
    {
      "from": "seed/_index.md",
      "to": "/agent/brain/brand-audit/_index.md",
      "on_exists": "skip",
      "purpose": "Local routing index for saved brand bundles"
    },
    {
      "appends_to": "/agent/INDEX.md",
      "guard": "path '/agent/brain/brand-audit/' already present",
      "purpose": "Global INDEX.md routing entry for the brand-audit library"
    }
  ],
  "runtime_installs": [
    {
      "appends_to": "/agent/user.md",
      "guard": "sentinel 'runneth-apps:brand-audit:read-before-creative v2' already present; removes any older v1 block",
      "trigger": "After the first successful brand-audit for any workspace, idempotent across subsequent runs",
      "purpose": "Standing instruction so all creative work reads the workspace's brand bundle as upstream context — narrowly, not as a full re-read"
    }
  ],
  "post_install": [
    {
      "action": "show-intro",
      "name": "Introduce the new capability",
      "file": "post-install-intro.md",
      "description": "Read post-install-intro.md from this use case folder. After all prior install and post-install steps complete, surface the file's sections to the user in chat as the closing message of the install turn: '## What just opened up' as prose, '## Try this now' as a numbered prompt list, '## Compounds with' only when present and at least one named use case is installed in the sandbox. Resolve any {{TOKEN}} placeholders using the same customize map applied during install."
    }
  ],
  "creates_at_runtime": [
    "/agent/brain/brand-audit/<workspace-slug>/brand-context.md",
    "/agent/brain/brand-audit/<workspace-slug>/product-catalog.md",
    "/agent/brain/brand-audit/<workspace-slug>/review-audit.md",
    "/agent/brain/brand-audit/<workspace-slug>/keywords.md",
    "/agent/brain/brand-audit/<workspace-slug>/competitor-analysis.md",
    "/agent/brain/brand-audit/<workspace-slug>/strategy.md",
    "/agent/brain/brand-audit/<workspace-slug>/_history/<archived-iso>/ (on re-run)"
  ],
  "upstream_dependencies": [],
  "changelog": [
    {
      "version": "2.0.0",
      "date": "2026-06-12",
      "type": "major",
      "notes": "Trimmed to the on-demand core. Cut the auto-invoked setup skill (setup-brand-audit no longer exists; the orchestrator asks the two or three intake questions — brand site, review sources, competitors — inline in chat on first run), the Monday 08:00 refresh routine, the scheduled Slack diff posts with ✅/❌/❓ reaction handling, and the per-workspace _config JSON (intake answers are now recorded inside the bundle files themselves and re-read on refresh). Refresh is on demand: re-running the skill archives the prior bundle to _history/ and shows a short diff in chat; rollback is a manual restore from history. The six standalone skills, the durable six-file bundle, and _history/ archiving are unchanged. user.md snippet bumped to runneth-apps:brand-audit:read-before-creative v2, which removes any older v1 block and points refresh guidance at on-demand re-runs instead of the Monday routine."
    },
    {
      "version": "1.0.1",
      "date": "2026-05-21",
      "type": "patch",
      "notes": "Added post-install intro. After install completes, Runneth surfaces a short 'what just opened up + try this now' message sourced from post-install-intro.md."
    },
    {
      "version": "1.0.0",
      "date": "2026-05-20",
      "type": "major",
      "notes": "Initial release. Six skills lifted from Alysha's creative-strategy framework (Foundation + Strategy layers). Each skill installs standalone at /agent/.agents/skills/<skill-name>/SKILL.md so it is independently auto-discoverable. Orchestrator runs them in order and writes a durable bundle under /agent/brain/brand-audit/<workspace-slug>/. Monday morning refresh routine baked in. user.md runtime install (runneth-apps:brand-audit:read-before-creative v1) so all creative work reads the bundle narrowly without re-running brand-audit."
    }
  ]
}
