{
  "schema": "1.0",
  "id": "aligned-onboarding",
  "version": "1.0.1",
  "description": "Teaches Runneth how a customer reads their Meta ad account, then builds one enriched record per active creative. Stages three instruction docs into the brain (Account Context Brain, Creative Corpus playbook, Motion CLI data-query guide), a thin orchestration skill, and a deterministic exporter that preserves all requested creative-summary content plus protected Account Context naming and Spend State projections. Activation merges a read-before-performance guard into /agent/user.md and runs the guided nine-field fill-in. Reuses the corpus-search tool from the library for filterable retrieval rather than bundling it. Meta only.",
  "requires": {
    "runtime": "any Runneth sandbox",
    "preinstalled": [],
    "recommended": [
      "A Motion workspace with a connected Meta ad account"
    ]
  },
  "depends": [],
  "customize": [
    {
      "token": "<workspaceId>",
      "purpose": "The Meta workspace this account context describes. Used in the /agent/user.md guard block and in every --workspace-id pull during the fill-in. Resolved during activation, not at file-copy time."
    }
  ],
  "installs": [
    {
      "from": "SKILL.md",
      "to": "/agent/.agents/skills/aligned-onboarding/SKILL.md",
      "on_exists": "overwrite",
      "purpose": "Orchestration skill. Activates the account-context guard, runs the nine-field fill-in, then builds and maintains the creative corpus. Triggered by prompts like 'run aligned onboarding' or 'set up my Meta account context'."
    },
    {
      "from": "bin/export-creative-corpus.mjs",
      "to": "/agent/.agents/skills/aligned-onboarding/bin/export-creative-corpus.mjs",
      "on_exists": "overwrite",
      "purpose": "Deterministic exporter for Motion Meta insights JSON. Validates every non-null summary section, writes one full-fidelity Markdown record per creative, and preserves its protected Account Context naming and Spend State projection on replacement."
    },
    {
      "from": "README.md",
      "to": "/agent/brain/aligned-onboarding/README.md",
      "on_exists": "overwrite",
      "purpose": "Package overview: how the two parts relate and the install/run order."
    },
    {
      "from": "account-context-brain.md",
      "to": "/agent/brain/aligned-onboarding/account-context-brain.md",
      "on_exists": "overwrite",
      "purpose": "The Account Context Brain fill-in package: nine required interpretation fields, how Runneth pulls and confirms them, and the prose output shape saved to /agent/brain/meta/account-context.md."
    },
    {
      "from": "creative-corpus-playbook.md",
      "to": "/agent/brain/aligned-onboarding/creative-corpus-playbook.md",
      "on_exists": "overwrite",
      "purpose": "The Creative Corpus playbook: how to build and maintain one enriched Markdown record per active creative under /agent/brain/meta/creatives/."
    },
    {
      "from": "motion-cli-data-query-guide.md",
      "to": "/agent/brain/aligned-onboarding/motion-cli-data-query-guide.md",
      "on_exists": "overwrite",
      "purpose": "Supporting reference: the canonical contract for pulling Meta data through the motion CLI. Brand-agnostic, carries no account IDs."
    }
  ],
  "changelog": [
    {
      "version": "1.0.1",
      "date": "2026-07-22",
      "type": "patch",
      "notes": "Creative Corpus pulls and enriched transcript retries request adDescription, hookOrHeadline, creativeBreakdown, and messagingAndPositioning explicitly. The installed exporter validates their stringified payloads, preserves the complete creative breakdown and messaging content, and retains protected Account Context naming and custom-threshold Spend State projections on replacement."
    },
    {
      "version": "1.0.0",
      "date": "2026-07-21",
      "type": "major",
      "notes": "Initial release. Aligned Onboarding teaches Runneth how a customer reads their Meta ad account and then builds a per-creative corpus off that lens. It ships two parts: the Account Context Brain (nine interpretation fields Runneth auto-pulls, confirms with a person, validates, and flags, then saves as a plain-language reference at /agent/brain/meta/account-context.md) and the Creative Corpus playbook (one enriched Markdown record per active creative with identity, summary, hook, transcript, value props, and Motion AI tags, under /agent/brain/meta/creatives/). A supporting Motion CLI data-query guide keeps the auto-pulls correct on the first try. Activation merges a workspace-scoped read-before-performance guard into /agent/user.md so the lens is loaded before any Meta performance work, and treats Motion workspace settings (goal, preferred KPI, spend threshold, attribution config) as if they do not exist. Meta only, one workspace at a time. Retrieval is automatic through Knoweth; the corpus-search tool is fetched from the library only if absent for deliberate filterable search, never bundled or clobbered."
    }
  ]
}
