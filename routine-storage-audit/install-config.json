{
  "schema": "1.0",
  "id": "routine-storage-audit",
  "version": "1.0.1",
  "description": "Audits an existing Runneth org for misplaced routine data and shared-database violations. Inventories every app and routine, classifies routines against the storage rules, and produces a migration plan file. Plan only, never modifies state.",
  "requires": {
    "runtime": "any Runneth sandbox",
    "preinstalled": [],
    "recommended": []
  },
  "installs": [
    {
      "file": "SKILL.md",
      "destination": "/agent/.agents/skills/routine-storage-audit/SKILL.md",
      "on-exists": "overwrite"
    },
    {
      "file": "references",
      "destination": "/agent/.agents/skills/routine-storage-audit/references",
      "type": "folder-copy",
      "on-exists": "overwrite"
    },
    {
      "file": "templates",
      "destination": "/agent/.agents/skills/routine-storage-audit/templates",
      "type": "folder-copy",
      "on-exists": "overwrite"
    }
  ],
  "changelog": [
    {
      "version": "1.0.1",
      "date": "2026-05-21",
      "type": "patch",
      "notes": "Added post-install intro. After install completes, Runneth surfaces a short 'what just opened up + try this now' message sourced from post-install-intro.md."
    },
    {
      "version": "1.0.0",
      "date": "2026-05-21",
      "type": "major",
      "notes": "Initial install-config. Skill + references + templates now installable via the standard protocol."
    }
  ],
  "post-install": [
    {
      "action": "show-intro",
      "name": "Introduce the new capability",
      "file": "post-install-intro.md",
      "description": "Read post-install-intro.md from this use case folder. After all prior install and post-install steps complete, surface the file's sections to the user in chat as the closing message of the install turn: '## What just opened up' as prose, '## Try this now' as a numbered prompt list, '## Compounds with' only when present and at least one named use case is installed in the sandbox. Resolve any {{TOKEN}} placeholders using the same customize map applied during install."
    }
  ]
}
