{
  "schema": "1.0",
  "id": "brain-onboard",
  "version": "1.7.0",
  "description": "Customer-side skill that turns a brain package prepared by a Motion CSM into a populated org brain. Reads the package from ./uploads/ or /agent/brain/_sources/, runs live pulls against the customer's Motion workspace plus customer-wired integrations like Yotpo, runs Apify scrapers for organic web context, and synthesizes ten brain domains with source citations on every line. Triggered by phrases like 'build my brain' or 'run brain-onboard' after the CSM uploads the package.",
  "requires": {
    "runtime": "any Runneth sandbox",
    "preinstalled": [],
    "recommended": []
  },
  "depends": [],
  "customize": [],
  "installs": [
    {
      "from": "SKILL.md",
      "to": "/agent/.agents/skills/brain-onboard/SKILL.md",
      "on_exists": "overwrite",
      "purpose": "Core brain-onboard skill. Triggered by user prompts like 'build my brain' or 'run brain-onboard' after the CSM uploads the brain package."
    },
    {
      "from": "seed/HOW-TO-USE.md",
      "to": "/agent/brain/runneth-onboarding/HOW-TO-USE.md",
      "on_exists": "overwrite",
      "purpose": "Plain-language guide for the team on what to do when the CSM sends them a brain package."
    }
  ],
  "changelog": [
    {
      "version": "1.7.0",
      "date": "2026-06-03",
      "type": "minor",
      "notes": "Canonicalize the signals/ layer surfaced during the Printfresh pilot. Eight standard files with brand-agnostic schemas and deterministic population rules: format-gaps (product \u00d7 format whitespace), audience-gaps (untargeted segments \u00d7 product fits), inspo-steals (competitor angles not in own rotation), seasonal-whitespace (upcoming occasions with no creative), review-requests (customer asks from VoC), organic-paid-gaps (organic themes absent from paid), hook-vocabulary (competitor language territories), graveyard-candidates (paused concepts to reconsider). Each file populates per install using the same logic against the brand's actual synthesized content. New Step 4.X documents the directory layout, common _meta frontmatter, per-file population rules, status semantics (populated/partial/empty-needs-upstream-content), refresh integration with routines, and briefing-chain integration. Empty signal files surface on the welcome card and in onboarding-checklist as \"connect <source> to unlock <signal>\". Welcome card Step 6 readout adds a Signals section sitting alongside the ten domains. Schemas live in the customer_brain repo at _signals-schemas/, not inlined in this skill. Custom per-customer schemas live at <customer-slug>/_signals-custom/. brain-onboard clones both at install and every refresh. This means new canonical signal types ship via PR to the customer_brain repo without needing a brain-onboard upgrade. Populated signals stay local at /agent/brain/signals/ in the customer Runneth (no push-back to repo since customer Runneths only have read access)."
    },
    {
      "version": "1.5.0",
      "date": "2026-06-03",
      "type": "minor",
      "notes": "Replace welcome card structure: drop the \"What it knows by heart\" snapshot section and expand the per-domain readout to carry the same high-signal facts. New structure: (1) Header, (2) \"What's in your brain by domain\" with one section per domain leading with a headline number and 2-3 highlights, (3) Capability cards by role, (4) \"What's not in the brain yet.\" The per-domain readout is the same information the old snapshot covered (KPI, top performer, headcount, brand positioning, customer voice, dominant hook) but anchored to where each fact lives in the brain. Each domain section ends with the brain path so the team knows where to look. Required per-domain content shape documented: identity (teammates + roles), brand (positioning + voice + products), customers (personas + themes + review counts), competition (confirmed brands + ad counts), performance (creatives + spend + KPI + top performer + best hook + best format + trend), strategy (KPI + windows + bets), calendar/library (pending OAuths), knowledge (glossary + decisions), preferences (empty on day one). Driven by Ankhway pilot."
    },
    {
      "version": "1.4.0",
      "date": "2026-06-03",
      "type": "minor",
      "notes": "Three changes: (1) Restore GitHub clone path in Step 1 as the PRIMARY package-staging path. Customer Runneth clones from Motion-Creative/customer_brain/<customer-slug>/ via CUSTOMER_BRAIN_READ_PAT. Tar and Drive paths stay as legacy fallbacks. (2) Remove motion meta age-gender from Step 2 pull list and remove age-gender.json from Performance domain inputs. (3) Add explicit never-run rule for Meta demographic analysis: demographics not load-bearing for creative strategy; personas come from VoC, not Meta-targeting. Supersedes the GitHub transport removal from PR #119; we are restoring it now that the CUSTOMER_BRAIN_READ_PAT secret is wired and the customer_brain repo flow is the canonical path (matches Ankhway example)."
    },
    {
      "version": "1.3.0",
      "date": "2026-06-03",
      "type": "minor",
      "notes": "Six changes: (1) Add Step 1.5 \u2014 explicit install of bundled skills from _skills/. (2) Add fourth install-state case to Step 3.5: _state.json present but content lacks frontmatter triggers upgrade-safe path (surfaced by Printfresh install). (3) Make _changelog.md write requirement explicit in Step 4 with format example. (4) Add Brand domain products/ substructure: per-product spec.md, claims.md, catalogue README with sources from brand-audit, web crawl, HubSpot, Shopify. (5) Add Customers domain substructure: personas/<slug>.md per persona, reviews/by-source/by-product/by-theme/, ad-comments/by-creative/by-theme/ with top-20 sampling and volume management. (6) Add Performance domain aggregations: by-hook-tactic, by-hook-voice, by-visual-format, by-messaging-angle, by-persona, by-awareness-stage using motion ai-glossary tags joined to motion meta/tiktok insights."
    },
    {
      "version": "1.2.0",
      "date": "2026-06-03",
      "type": "minor",
      "notes": "Two changes: (1) Drop GitHub repo transport from Step 1 (supersedes PR #118). (2) Add Step 3.5 \u2014 upgrade detection and existing-content protection. When brain-onboard installs on a Runneth that already has brain content from an older version, the upgrade-safe path archives existing content, walks for user-saved candidates, surfaces findings on the welcome card for explicit team confirmation, and re-imports approved files with managed_by: user frontmatter before synthesis runs. Prevents silent loss of save-this-to-brain content during version upgrades."
    },
    {
      "version": "1.0.0",
      "date": "2026-06-02",
      "type": "major",
      "notes": "Initial release. Customer-side skill that turns a CSM-prepared brain package into a populated ten-domain org brain. Reads the package from uploads or sources directory, runs Motion workspace pulls, runs Apify scrapers, pulls customer-wired integrations like Yotpo, and synthesizes the brain with source citations. Welcome card leads with what each person on the team can now do."
    }
  ]
}