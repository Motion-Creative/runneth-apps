{
  "schema": "1.0",
  "id": "add-roles-permissions",
  "version": "3.0.0",
  "description": "Lock the areas of your team's brain that only certain people should be editing. Brand strategy, pricing, client positioning, anything sensitive. A short conversation captures what to protect and who owns each area, then Runneth blocks unauthorized edits and routes change requests through your approval channel. Every save in your brain gets stamped with who wrote it. New teammates onboard automatically.",
  "requires": {
    "runtime": "any Runneth sandbox; jq must be installed",
    "preinstalled": [],
    "recommended": []
  },
  "installs": [
    {
      "file": "SKILL.md",
      "destination": "/agent/.agents/skills/add-roles-permissions/SKILL.md",
      "on-exists": "overwrite"
    }
  ],
  "post-install": [
    {
      "action": "skill-invoke",
      "skill": "add-roles-permissions",
      "name": "Run the five-phase deployment",
      "description": "Invokes the skill's full deployment: pre-flight scan, summary + confirmation, file writes, post-deploy verification, post-deploy setup checklist.",
      "manual": false
    },
    {
      "action": "show-intro",
      "name": "Introduce the new capability",
      "file": "post-install-intro.md",
      "description": "Read post-install-intro.md from this use case folder. After all prior install and post-install steps complete, surface the file's sections to the user in chat as the closing message of the install turn: '## What just opened up' as prose, '## Try this now' as a numbered prompt list, '## Compounds with' only when present and at least one named use case is installed in the sandbox. Resolve any {{TOKEN}} placeholders using the same customize map applied during install."
    }
  ],
  "changelog": [
    {
      "version": "3.0.0",
      "date": "2026-06-02",
      "type": "major",
      "notes": "Major rewrite around six primitives instead of two modes. The permissive-vs-strict framing is gone. The system is now built from people (access registry), spaces (folders in the brain), writers per space (everyone | specific | admins only, default everyone), attribution (always on), approval routing (optional Slack channel), and identity resolution (Neon-only). The conversation discovers the org's strategy and writes a tailored spaces.json. Each space gets its own writer rule \u2014 wide-open and locked-down spaces can coexist in the same install. No mode.json, no template selection in permissions.md (single template generated from spaces.json). Reconfigure is incremental ('let's tighten up the client space', 'open up the shared space', 'add a new space') instead of a mode switch. v2.x migration reads existing permissions rules into the new spaces model. The Phase 3 conversation barely changes from earlier drafts: same six things to listen for (team, work shape, what to keep organized, areas only certain people should change, areas anyone can contribute to, first admin) plus the approval-channel follow-up. Phase 4 readback in plain language. Phase 5 deployment uses a single permissions.md template populated from spaces.json. Phase 7 setup checklist is one consistent message. Step 8 auto-cleans the team-member-memory v2.0.1 user.md leak if found. Refs: PDEC-7817."
    },
    {
      "version": "2.0.0",
      "date": "2026-06-02",
      "type": "major",
      "notes": "Two changes: (1) renamed workspace-map.json to organization-map.json (clearer: it is the organization-wide identity registry, not a workspace-scoped artifact); env override variable renamed RUNNETH_WORKSPACE_MAP to RUNNETH_ORG_MAP. (2) motion-whoami.sh is now Neon-only: queries the Neon agent_conversation table via a new motion-whoami-neon.py helper (invoked through secret run --env DATABASE_URL=NEON_DATABASE_URL) for authoritative, zero-lag identity resolution. No SQLite fallback \u2014 on Neon failure the script exits non-zero and the permissions layer treats the user as unknown (no writes) per the \u00a72.Unknown rule. Accepts CONVERSATION_ID from env in addition to cwd-basename. Fixes the failure mode where brand-new conversations could not be resolved because the live conversations.db is a 0-byte placeholder until the backup cycle. Migration of existing orgs is deferred. Refs: PDEC-7817."
    },
    {
      "version": "1.0.1",
      "date": "2026-05-21",
      "type": "patch",
      "notes": "Added post-install intro. After install completes, Runneth surfaces a short 'what just opened up + try this now' message sourced from post-install-intro.md."
    },
    {
      "version": "1.0.0",
      "date": "2026-05-21",
      "type": "major",
      "notes": "Initial install-config. Replaces ad-hoc skill-invoke install with the standard protocol."
    }
  ]
}
