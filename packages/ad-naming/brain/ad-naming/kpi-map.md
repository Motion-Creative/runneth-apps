# Per-Campaign KPI Map

_Not yet built. Run "build my naming decoder" — the KPI map is built as part of that process._

---

This file maps each campaign segment to its optimization target, primary conversion event, and decision rules. It supplements the primary KPI goal in Context Kit with account-specific per-campaign logic.
